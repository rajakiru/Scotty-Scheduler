# Streamlit Secrets Configuration
# Copy this file and create your actual secrets in Streamlit Cloud dashboard

# API Configuration
OPENAI_API_KEY = "sk-your-openai-api-key-here"

# Backend API endpoint (use this if backend is hosted separately)
# API_ENDPOINT = "https://your-backend-url.onrender.com/query"

# If running backend locally or not deployed yet, leave commented
# API_ENDPOINT = "http://127.0.0.1:5000/query"
